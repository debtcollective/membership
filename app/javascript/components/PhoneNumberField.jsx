import React from 'react'
import PhoneInput from 'react-phone-number-input'

const PhoneNumberField = (props = {}) => {
  return <PhoneInput onChange={() => {}} {...props} />
}

export default PhoneNumberField
